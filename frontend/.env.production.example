# Copy to frontend/.env.production for production builds (gitignored).
# Public API base URLs only — do not put JWT secrets here.

VITE_API_URL=https://YOUR-API-HOST/api/v1
VITE_NATIVE_API_URL=https://YOUR-API-HOST/api/v1
